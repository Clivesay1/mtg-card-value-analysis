# Notebook Guide

Use `mtg_value_scoring_model.ipynb` as the supported, end-to-end analysis entry point. It loads the committed data snapshot, performs exploratory analysis and model evaluation, and generates the project outputs.

The repository previously contained empty phase-notebook placeholders. They have been removed to avoid presenting invalid notebook files as runnable analysis steps.
